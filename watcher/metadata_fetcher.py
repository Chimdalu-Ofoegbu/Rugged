"""Rugged · commit watcher — metadata fetcher.

Phase 2. Given a coin address + chain, fetches current price (Pyth, else
CoinGecko), LP info, decimals, and pair address. Stores the blacklist-time
price — the resolution baseline.

STUB — implemented in Phase 2 (project.md §"Phase 2: Commit Watcher").
"""
