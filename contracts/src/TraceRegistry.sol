// SPDX-License-Identifier: MIT
pragma solidity ^0.8.28;

/// @title  TraceRegistry
/// @notice Permanent on-chain audit trail. Stores the SHA-256 hash and IPFS
///         CID of each market's full swarm reasoning trace.
/// @dev    STUB — implemented in Phase 1 (project.md §1e):
///         registerTrace(uint256 marketId, bytes32 traceHash, string ipfsCid);
///         TraceRegistered event; operator-only.
contract TraceRegistry {
    // Phase 1
}
