"""Rugged · swarm — consensus.

Phase 3. Takes the three agent outputs. If >=2 of 3 scores exceed 0.5,
consensus fires. Seed probability = mean of the three scores (in bps).
Assembles the full swarm trace JSON and returns
{fire: bool, seed_probability_bps: int, trace: {...}}.

STUB — implemented in Phase 3 (project.md §"Phase 3: Three Agents").
"""
