# Privy + viem + permissionless declare overlapping peer-dep ranges that
# don't fully reconcile under strict resolution. The same lockfile installs
# cleanly with --legacy-peer-deps locally; we enable it project-wide so
# `npm ci` in CI matches local `npm install` behavior.
legacy-peer-deps=true
