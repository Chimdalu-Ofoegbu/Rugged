# Personal Claude Code settings (user-specific, not for the repo)
.claude/settings.local.json

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Secrets — never commit
.env
.env.local
# Circle entity-secret recovery file
recovery/

# Foundry / smart contracts
contracts/out/
contracts/cache/
contracts/broadcast/

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
# note: uv.lock IS committed (reproducible installs)

# Node / frontend
node_modules/
.next/
dist/

# Superseded pitch reference (Phase 6 uses the existing project/ UI)
rugged-pitch.html

# Runtime state — regenerated per deploy, never commit
data/telegram_subscribers.json
data/telegram_alerted_markets.json
data/demo_wallet.json
# Per-user wallet records (testnet addresses, but still session state)
data/wallets/
# Faucet rate-limit ledgers
data/faucet_ledger.json
data/faucet_ip_ledger.json
# Resolver daemon per-market price-tracking state
data/price_tracking/

# Foundry / Anvil lock file
contracts/foundry.lock
